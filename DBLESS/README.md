# kong-gateway
Basic Kong gateway using config file

## set up 
Make sure you have:
* docker 
* docker compose  
`git clone https://github.com/brayomumo/kong-gateway`  
`cd kong-gateway`   
`docker-compose up`

